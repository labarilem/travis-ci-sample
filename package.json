{
  "name": "travis-ci-sample",
  "description": "Simple Travis-CI check for JSHint (Code Linting)",
  "author": "labarilem",
  "version": "0.0.1",
  "devDependencies": {
    "jshint": "^2.6.0"
  },
  "scripts": {
    "setup": "npm i && echo custom_install",
    "test": "jshint hello.js",
    "ci": "npm run test && echo custom_test"
  }
}